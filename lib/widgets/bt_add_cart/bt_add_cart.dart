import 'package:flutter/material.dart';

class BtAddCart extends StatelessWidget {
  const BtAddCart({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(onPressed: () {}, child: const Text('Add to Cart'));
  }
}
